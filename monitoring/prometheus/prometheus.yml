global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: 'mgstore-backend'
    metrics_path: /api/actuator/prometheus
    static_configs:
      - targets: ['backend:8891']
